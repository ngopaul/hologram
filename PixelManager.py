# Manages pixels, using Modeler.py to get information about what each pixel should try to output.
# It then turns this into an ideal path which the pixels must follow to create the model.
# Paths are in the form of a list of lists: [ [duration phi theta] [duration phi theta] ... ]